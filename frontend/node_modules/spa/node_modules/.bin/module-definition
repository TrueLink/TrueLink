#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../module-definition/bin/module-definition.js" "$@"
  ret=$?
else 
  node  "$basedir/../module-definition/bin/module-definition.js" "$@"
  ret=$?
fi
exit $ret
