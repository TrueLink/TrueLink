#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../detective-amd/bin/detective-amd.js" "$@"
  ret=$?
else 
  node  "$basedir/../detective-amd/bin/detective-amd.js" "$@"
  ret=$?
fi
exit $ret
